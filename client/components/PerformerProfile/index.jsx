/* eslint-disable react/prop-types */
import React from 'react';

const PerformerProfile = () => {

  // const {
  //   name, performer, followers, hostedEvents, bio, cashappURL, zipcode, photo, email,
  // } = profileInfo;

  // const events = hostedEvents.map((event) => (
  //   <div key={event._id}>{`Upcoming Performances: ${event.description} at ${event.time}`}</div>
  // ));

  return (
    <div>PERFORMER INFO GOES HERE</div>
    // <div>
    //   <h3>Account Settings</h3>
    //   <button type="button">edit profile</button>
    //   <img src={photo} alt="profile avatar" />
    //   <div>{`Name: ${name}`}</div>
    //   <div>{`payment: ${cashappURL}`}</div>
    //   <div>{`Email: ${email}`}</div>
    //   <div>{`Bio: ${bio}`}</div>
    //   {performer
    //     ? (
    //       <>
    //         <div>{`Follower Count: ${followers}`}</div>
    //         <div>{`Home Zipcode: ${zipcode}`}</div>
    //         {events}
    //       </>
    //     )
    //     : <div>{`Home Zipcode: ${zipcode}`}</div>}
    // </div>
  );
};
export default PerformerProfile;
